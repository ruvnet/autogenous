[package]
name = "witness"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Autogenous witness plane: content-addressed hashing (SHA-256 over canonical JSON) + ed25519 signing/verification + append-only witness records with separate signing authorities (ADR-392 §13, gap 3)."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
ed25519-dalek = { workspace = true }
